// Create the base div that shells the app's View content
export const baseDiv = document.createElement('div');
baseDiv.id = 'body';
baseDiv.classList.add('dynamic-attributes');
document.body.appendChild(baseDiv);